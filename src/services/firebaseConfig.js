import firebase from '@react-native-firebase/app';
import auth from '@react-native-firebase/auth';

// Khởi tạo Firebase (thường không cần cấu hình thủ công nếu dùng google-services.json)
export { auth };